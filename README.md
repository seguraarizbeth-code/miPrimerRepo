# Git Folw Basico
 comando para el flujo git basico
 ```sh
 git status
 git add . o git add -a
 git status
 git commit -a "Descripcion del cambio del proyecto"
 git push


 ##Si no me dice quien soy,entonces necesito restaurar la PC.

